# Placeholder images to replace

The following PNGs in this directory are 1200×600 grey placeholders. Replace each with a real screenshot captured while walking through Part 2 of the Fabric Quickstart workshop.

| File                         | What to capture                                                                                                                                                           |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `02-get-data-dialog.png`     | The "Get data in your lakehouse" dialog in a newly created Fabric Lakehouse (cards: Upload files, Start with sample data, New shortcut, New Dataflow Gen2, New copy job). |
| `02-table-preview.png`       | The Lakehouse explorer with the `publicholidays` sample table selected, showing its preview rows.                                                                         |
| `02-sql-query.png`           | The SQL analytics endpoint query editor after running the `SELECT TOP 10 countryOrRegion, COUNT(*) ...` query, with results visible.                                      |
| `03-upload-files.png`        | Lakehouse Files folder with `sales.csv` visible after upload.                                                                                                             |
| `03-load-to-table.png`       | The "Load to table" dialog with table name `sales` and file type CSV.                                                                                                     |
| `03-sales-table-preview.png` | The `sales` table selected in Lakehouse explorer showing preview rows.                                                                                                    |
| `03-delta-table-files.png`   | The Files view of the `sales` Delta table showing `.parquet` file(s) and the `_delta_log/` subfolder (reached via `…` → **View files** on the table).                     |

Delete this file once all images are replaced.
